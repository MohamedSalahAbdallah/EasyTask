export interface Itask {
  id: string;
  userId: string;
  title: string;
  summary: string;
  dueDate: string;
}
export interface INewTask {
  userId: string;
  title: string;
  summary: string;
  dueDate: string;
}
